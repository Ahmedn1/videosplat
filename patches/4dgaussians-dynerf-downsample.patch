diff --git a/scene/dataset_readers.py b/scene/dataset_readers.py
index 8a6cc42..45f03e4 100644
--- a/scene/dataset_readers.py
+++ b/scene/dataset_readers.py
@@ -443,19 +443,21 @@ def readdynerfInfo(datadir,use_bg_points,eval):
     # ply_path = os.path.join(datadir, "points3D_dense.ply")
     ply_path = os.path.join(datadir, "points3D_downsample2.ply")
     from scene.neural_3D_dataset_NDC import Neural3D_NDC_Dataset
+    import os as _os
+    _downsample = float(_os.environ.get("DYNERF_DOWNSAMPLE", "4.0"))
     train_dataset = Neural3D_NDC_Dataset(
     datadir,
     "train",
-    1.0,
+    _downsample,
     time_scale=1,
     scene_bbox_min=[-2.5, -2.0, -1.0],
     scene_bbox_max=[2.5, 2.0, 1.0],
     eval_index=0,
-        )    
+        )
     test_dataset = Neural3D_NDC_Dataset(
     datadir,
     "test",
-    1.0,
+    _downsample,
     time_scale=1,
     scene_bbox_min=[-2.5, -2.0, -1.0],
     scene_bbox_max=[2.5, 2.0, 1.0],
